const root = "";

const routinesRoot = `${root}/routines`;

const routines = {
  root: routinesRoot,
  add: `${routinesRoot}/add`
};

const routing = {
  root,
  home: `${root}/home`,
  login: `${root}/login`,
  signup: `${root}/signup`,
  dashboard: `${root}/dashboard`,
  routines
};
